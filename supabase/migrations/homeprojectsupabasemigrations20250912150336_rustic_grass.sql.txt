/*
  # Add estoque type to Lanches table

  1. Changes to Table
    - Update CHECK constraint to include 'estoque' type
    - This allows the system to handle inventory control entries

  2. Security
    - No changes to RLS policies needed
*/

-- Update the CHECK constraint to include 'estoque' type
ALTER TABLE "Lanches" DROP CONSTRAINT IF EXISTS "Lanches_tipo_check";
ALTER TABLE "Lanches" ADD CONSTRAINT "Lanches_tipo_check" 
  CHECK (tipo IN ('lanche', 'perda', 'sobra', 'transferencia', 'estoque'));